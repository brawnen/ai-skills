interface:
  display_name: "Release Scope Check"
  short_description: "核对多模块发布范围和验证步骤"
  brand_color: "#2563EB"
  default_prompt: "Use $project-release-scope-check to check which modules must be released, conditionally released, or skipped."
policy:
  allow_implicit_invocation: true
